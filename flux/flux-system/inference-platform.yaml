# flux/flux-system/inference-platform.yaml
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: inference-platform
  namespace: flux-system
spec:
  interval: 5m
  path: ./k8s/base/inference-platform
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  targetNamespace: inference-platform
